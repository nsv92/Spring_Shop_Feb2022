server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/SPRING_SHOP_FEB22
    username: root
    password: root
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: validate
  thymeleaf:
    enabled: true
    encoding: UTF-8
#    check-template-location: true
#    prefix: classpath:/templates/
#    suffix: .html